# Áudio capturado e transcrições — NUNCA versionar (conteúdo privado/pesado)
*.wav
*.m4a
*.mp3
*.flac
*.txt.transcript
/staging/
/Gravacoes/
/recordings/

# Segredos / config local
.env
*.local
config.local.*
auth.json

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/
*.egg-info/
build/
dist/

# Node (caso o Hammerspoon/glue use)
node_modules/

# Build artifact do recorder nativo — gerado por recorder/build.sh
/recorder/voxlog-rec.app/
/recorder/voxlog-rec.bin

# macOS
.DS_Store
